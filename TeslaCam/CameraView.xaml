<UserControl x:Class="TeslaCam.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:TeslaCam"
             mc:Ignorable="d">
    <Border Width="256"
            Height="192"
            Background="#80212121">
        <Grid>
            <MediaElement Source="{Binding SourceUri, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          LoadedBehavior="Play"
                          Stretch="Uniform" />

            <TextBlock Text="{Binding FeedName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="White"
                       FontSize="20"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Bottom"
                       Margin="8" />
        </Grid>
    </Border>
</UserControl>
